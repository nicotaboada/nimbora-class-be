/**
 * Constantes de facturación para la generación de cargos.
 * En el futuro, estos valores serán configurables por academia.
 */

/** Día del mes en que se generan los cargos */
export const CHARGE_DAY_OF_MONTH = 1;

/** Días de gracia después de la fecha de emisión -> dueDate = issueDate + GRACE_DAYS */
export const GRACE_DAYS = 9;
